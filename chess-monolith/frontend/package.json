{
  "name": "chessemag-frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:lan": "vite --host 0.0.0.0",
    "build": "vite build && mkdir -p dist/js dist/images && cp -R js/. dist/js/ && cp -R images/. dist/images/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
